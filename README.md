# Firebase Studio

This is a NextJS starter in Firebase Studio.

To get started, take a look at src/app/page.tsx.
# AI-MOCK-INTERVIEW

Note on local skill extraction: If you run the Python backend (see `python-backend/README.md`) the app will call that service to extract skills from any text that was extracted from a PDF and pass those extracted skills to Gemini to improve question tailoring.

